---
title: "Swirl"
description: "Animated bands of color twisting and bending, producing spirals, arcs, and flowing circular patterns."
---

Animated bands of color twist and bend radially, producing spirals, concentric arcs, and flowing circular patterns. The twist exponent controls how sharply the bands curve toward the center. Simplex noise distortion, band count, and per-color softness give extensive stylistic control.

## Usage

<Tabs>
  <Tab title="React">
    ```tsx
    import { Swirl } from '@paper-design/shaders-react';

    export default function Example() {
      return (
        <Swirl
          colorBack="#330000"
          colors={['#ffd1d1', '#ff8a8a', '#660000']}
          bandCount={4}
          twist={0.1}
          center={0.2}
          proportion={0.5}
          softness={0}
          noiseFrequency={0.4}
          noise={0.2}
          speed={0.32}
          style={{ width: '100%', height: '100%' }}
        />
      );
    }
    ```
  </Tab>
  <Tab title="Vanilla JS">
    ```ts
    import { ShaderMount, swirlFragmentShader } from '@paper-design/shaders';

    const container = document.querySelector('#canvas-container');

    const shader = new ShaderMount(container, swirlFragmentShader, {
      u_colorBack: [0.200, 0, 0, 1],
      u_colors: [
        [1, 0.820, 0.820, 1],
        [1, 0.541, 0.541, 1],
        [0.400, 0, 0, 1],
      ],
      u_colorsCount: 3,
      u_bandCount: 4,
      u_twist: 0.1,
      u_center: 0.2,
      u_proportion: 0.5,
      u_softness: 0,
      u_noiseFrequency: 0.4,
      u_noise: 0.2,
    });
    ```
  </Tab>
</Tabs>

## Props

<ParamField path="colorBack" type="string" default="#330000">
  Background color shown outside the swirl.
</ParamField>

<ParamField path="colors" type="string[]" default='["#ffd1d1","#ff8a8a","#660000"]'>
  Array of up to 10 colors cycling across the swirl bands.
</ParamField>

<ParamField path="bandCount" type="number" default="4">
  Number of color bands. `0` produces concentric ripples. Range: `0` to `15`.
</ParamField>

<ParamField path="twist" type="number" default="0.1">
  Vortex power. `0` = straight sectoral shapes, higher = tightly wound spiral. Range: `0` to `1`.
</ParamField>

<ParamField path="center" type="number" default="0.2">
  How far from the center the swirl colors begin to appear. Range: `0` to `1`.
</ParamField>

<ParamField path="proportion" type="number" default="0.5">
  Blend point between colors. `0.5` = equal distribution. Range: `0` to `1`.
</ParamField>

<ParamField path="softness" type="number" default="0">
  Color transition sharpness. `0` = hard edge, `1` = smooth gradient. Range: `0` to `1`.
</ParamField>

<ParamField path="noise" type="number" default="0.2">
  Strength of simplex noise distortion on the bands. Range: `0` to `1`.
</ParamField>

<ParamField path="noiseFrequency" type="number" default="0.4">
  Frequency of the noise distortion. Has no effect when `noise` is `0`. Range: `0` to `1`.
</ParamField>

<ParamField path="speed" type="number" default="0.32">
  Animation speed multiplier. Set to `0` to pause.
</ParamField>

<ParamField path="frame" type="number" default="0">
  Static frame position when `speed` is `0`.
</ParamField>

## Presets

| Name | Description |
|------|-------------|
| `Default` | Red swirl bands on dark red |
| `007` | Classic gun-barrel spiral in black and cream |
| `Opening` | Soft warm iris-opening swirl |
| `Candy` | Pastel rainbow candy swirl |

## Sizing

This shader uses `ShaderSizingParams`. See [Sizing and fit](/concepts/sizing-and-fit) for `fit`, `scale`, `rotation`, `offsetX`, `offsetY`, `originX`, `originY`, `worldWidth`, and `worldHeight` props.
